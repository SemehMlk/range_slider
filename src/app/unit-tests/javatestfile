<?xml version="1.0" encoding="UTF-8"?>
<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.8.xsd">

    <changeSet id="modify-canonical-message-primary-key" author="your_name">
        
        <!-- 1️⃣ Suppression des clés étrangères pointant vers canonical_message.id -->
        <dropForeignKeyConstraint baseTableName="some_dependent_table" 
                                  constraintName="fk_some_table_canonical_message"/>

        <!-- 2️⃣ Suppression de la clé primaire actuelle -->
        <dropPrimaryKey tableName="canonical_message"/>

        <!-- 3️⃣ Suppression de la colonne `id` -->
        <dropColumn tableName="canonical_message">
            <column name="id"/>
        </dropColumn>

        <!-- 4️⃣ Ajout de la nouvelle PRIMARY KEY sur `inbound_message_id` -->
        <addPrimaryKey columnNames="inbound_message_id" 
                       tableName="canonical_message" 
                       constraintName="pk_canonical_message"/>

        <!-- 5️⃣ Réajout de la clé étrangère sur les tables dépendantes si nécessaire -->
        <addForeignKeyConstraint baseTableName="some_dependent_table"
                                 baseColumnNames="canonical_message_id"
                                 referencedTableName="canonical_message"
                                 referencedColumnNames="inbound_message_id"
                                 constraintName="fk_some_table_canonical_message"
                                 onDelete="CASCADE"/>

    </changeSet>

</databaseChangeLog>
