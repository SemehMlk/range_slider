import com.fasterxml.jackson.databind.ObjectMapper;
import java.time.LocalDateTime;
import java.time.format.DateTimeFormatter;
import java.util.List;

public class TsnPayloadInfoProcessor {

    public static TsnPayloadInfo processPayload(String json) {
        ObjectMapper objectMapper = new ObjectMapper();
        try {
            // Désérialiser le JSON en objet TsnPayloadInfo
            TsnPayloadInfo tsnPayloadInfo = objectMapper.readValue(json, TsnPayloadInfo.class);

            // Mettre à jour les propriétés selon le type
            if (tsnPayloadInfo.getProperties() != null) {
                for (TsnPayloadInfoProperty property : tsnPayloadInfo.getProperties()) {
                    if ("localDtm".equalsIgnoreCase(property.getType())) {
                        String currentDateTime = LocalDateTime.now()
                                .format(DateTimeFormatter.ofPattern("yyyy-MM-dd HH:mm:ss"));
                        property.setValue(currentDateTime);
                    }
                }
            }
            return tsnPayloadInfo;
        } catch (Exception e) {
            e.printStackTrace();
            return null;
        }
    }

    public static void main(String[] args) {
        // Exemple JSON
        String json = "{ \"request\": { \"name\": \"ExampleName\" }, \"properties\": [ { \"type\": \"localDtm\", \"value\": \"\" }, { \"type\": \"Type2\", \"value\": \"Value2\" } ] }";

        // Appel de la fonction et affichage du résultat
        TsnPayloadInfo result = processPayload(json);
        System.out.println("TsnPayloadInfo mis à jour : " + result);
    }
}


🔍 Explication des paramètres :
Paramètre	Valeur optimisée	Explication
registry-fetch-interval-seconds	15	Les services rafraîchissent leur liste de services disponibles toutes les 15s (au lieu de 30s).
lease-renewal-interval-in-seconds	15	Chaque service envoie son heartbeat toutes les 15s (au lieu de 30s).
lease-expiration-duration-in-seconds	45	Un service qui ne répond plus sera supprimé après 45s (au lieu de 90s).
fetch-registry: false	Désactivé	Évite que les services téléchargent toute la liste au démarrage (si non nécessaire).
enable-self-preservation	false	Désactive la protection automatique qui peut ralentir l'expiration des services morts.
eviction-interval-timer-in-ms	30000 (30s)	Accélère la suppression des services non répondants.

