import { Component } from '@angular/core';
import * as Papa from 'papaparse';

@Component({
  selector: 'app-root',
  templateUrl: './app.component.html',
  styleUrls: ['./app.component.css']
})
export class AppComponent {

  jsonData: any[] = [];
  
  // Liste des champs que vous voulez récupérer
  desiredFields = ['name', 'age', 'city', 'country'];  // Spécifiez ici les 4 champs

  // Cette méthode est déclenchée lorsque l'utilisateur sélectionne un fichier
  onFileSelected(event: any): void {
    const file: File = event.target.files[0];

    if (file) {
      // Appeler la fonction pour traiter le fichier CSV
      this.readCsvFile(file);
    }
  }

  // Lecture et conversion du CSV en JSON
  readCsvFile(file: File): void {
    const reader = new FileReader();
    reader.onload = () => {
      const csvData = reader.result;

      // Utilisation de PapaParse pour parser le CSV
      Papa.parse(csvData as string, {
        header: true, // Convertir la première ligne en propriétés d'objet
        skipEmptyLines: true,
        complete: (result) => {
          // Transformer le résultat pour ne garder que les champs désirés
          this.jsonData = result.data.map((row: any) => {
            const filteredRow: any = {};
            this.desiredFields.forEach(field => {
              if (row.hasOwnProperty(field)) {
                filteredRow[field] = row[field];
              }
            });
            return filteredRow;
          });
          
          console.log(this.jsonData); // Afficher les données filtrées en JSON
        }
      });
    };

    reader.readAsText(file);
  }
}
