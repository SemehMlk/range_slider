openapi: 3.0.0
info:
  title: Stream API
  description: API for creating and listing streams
  version: 1.0.0
components:
  schemas:
    TechnicalParameters:
      type: object
      properties:
        channel:
          type: string
        host:
          type: string
        mqUser:
          type: string
        mqPassword:
          type: string
        port:
          type: integer
          format: int32
      required:
        - channel
        - host
        - mqUser
        - port

    KemmMqManager:
      type: object
      properties:
        id:
          type: integer
          format: int64
        queueManager:
          type: string
        channel:
          type: string
        host:
          type: string
        mqUser:
          type: string
        port:
          type: integer
          format: int32

    KemmMqStream:
      type: object
      properties:
        id:
          type: integer
          format: int64
        upStreamQueue:
          type: string
        downStreamQueue:
          type: string
        replyToDownStreamQueue:
          type: string
        kemmMqManager:
          $ref: '#/components/schemas/KemmMqManager'

    KemmStream:
      type: object
      properties:
        id:
          type: integer
          format: int64
        alias:
          type: string
        type:
          type: string
        description:
          type: string
        status:
          type: string
        inputMessageType:
          type: string
        creationDate:
          type: string
          format: date-time
        mqStreamDetails:
          $ref: '#/components/schemas/KemmMqStream'

    CreateMqStreamRequest:
      type: object
      properties:
        alias:
          type: string
        type:
          type: string
        description:
          type: string
        status:
          type: string
        inputMessageType:
          type: string
        upStreamQueue:
          type: string
        downStreamQueue:
          type: string
        replyToDownStreamQueue:
          type: string
        technicalParameters:
          $ref: '#/components/schemas/TechnicalParameters'
      required:
        - alias
        - type
        - upStreamQueue
        - downStreamQueue
        - technicalParameters

    StreamList:
      type: array
      items:
        $ref: '#/components/schemas/KemmStream'





