public toggleEditableState(): void {
  // Vérifier si tous les critères sont valides et mettre à jour leur état
  this.criterias.forEach((criteria, index) => {
    if (this.isFilterValid(criteria)) {
      criteria.isEditable = false; // Si valide, rendre non éditable
    }
  });
}

