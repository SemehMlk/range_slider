 private buildFieldMap() {
    this.columnDefs.forEach(colDef => {
      const agGridField = colDef.field;
      const backendField = colDef.backendField || agGridField; // Utiliser backendField ou fallback au field
      this.fieldMap[agGridField] = backendField;
    });
  }
