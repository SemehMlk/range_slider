<databaseChangeLog
    xmlns="http://www.liquibase.org/xml/ns/dbchangelog"
    xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
    xsi:schemaLocation="http://www.liquibase.org/xml/ns/dbchangelog
        http://www.liquibase.org/xml/ns/dbchangelog/dbchangelog-4.5.xsd">

    <changeSet id="add-scope-id-to-canonical-message" author="dev">
        <!-- Ajout de la colonne scope_id -->
        <addColumn tableName="canonical_message">
            <column name="scope_id" type="BIGINT">
                <constraints nullable="true"/>
            </column>
        </addColumn>

        <!-- Ajout de la contrainte de clé étrangère -->
        <addForeignKeyConstraint
            baseTableName="canonical_message"
            baseColumnNames="scope_id"
            constraintName="fk_canonical_message_scope"
            referencedTableName="scope"
            referencedColumnNames="id"
            onDelete="SET NULL"/>
    </changeSet>

</databaseChangeLog>
